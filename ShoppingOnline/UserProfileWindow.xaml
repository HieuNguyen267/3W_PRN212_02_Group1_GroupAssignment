<Window x:Class="ShoppingOnline.UserProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ShoppingOnline"
        Title="Thông tin cá nhân" Height="750" Width="1200" 
        WindowStartupLocation="CenterScreen" ResizeMode="CanResize">
    
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryRed" Color="#E31837"/>
        <SolidColorBrush x:Key="LightGray" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="MediumGray" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="DarkGray" Color="#666666"/>
        
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource MediumGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="2" BlurRadius="8"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource DarkGray}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        
        <Style x:Key="InfoValueStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>
        
        <Style x:Key="StatCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="1" BlurRadius="4"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="OrderItemStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource MediumGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#10000000" Direction="270" ShadowDepth="1" BlurRadius="3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C41230"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        

    </Window.Resources>
    
    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryRed}" Padding="30,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Thông tin cá nhân" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    <TextBlock Text="Quản lý thông tin tài khoản và đơn hàng" Foreground="White" FontSize="16" Margin="0,8,0,0" Opacity="0.9"/>
                </StackPanel>
                
                <Button Grid.Column="1" Content="✕" Background="Transparent" Foreground="White" 
                        BorderThickness="0" FontSize="20" Click="Close_Click" Padding="12" 
                        FontWeight="Bold" Cursor="Hand"/>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="30">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Left Sidebar - User Info -->
                <StackPanel Grid.Column="0" Margin="0,0,25,0">
                    <!-- User Profile Card -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="👤 Thông tin cá nhân" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="Họ và tên:" Style="{StaticResource InfoLabelStyle}"/>
                                <Border Grid.Row="0" Grid.Column="1" Background="{StaticResource LightGray}" CornerRadius="6" Padding="12,8" Margin="0,0,0,15">
                                    <TextBlock Text="{Binding CustomerName}" Style="{StaticResource InfoValueStyle}" Margin="0"/>
                                </Border>
                                
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="Email:" Style="{StaticResource InfoLabelStyle}"/>
                                <Border Grid.Row="1" Grid.Column="1" Background="{StaticResource LightGray}" CornerRadius="6" Padding="12,8" Margin="0,0,0,15">
                                    <TextBlock Text="{Binding Email}" Style="{StaticResource InfoValueStyle}" Margin="0"/>
                                </Border>
                                
                                <TextBlock Grid.Row="2" Grid.Column="0" Text="Số điện thoại:" Style="{StaticResource InfoLabelStyle}"/>
                                <Border Grid.Row="2" Grid.Column="1" Background="{StaticResource LightGray}" CornerRadius="6" Padding="12,8" Margin="0,0,0,15">
                                    <TextBlock Text="{Binding Phone}" Style="{StaticResource InfoValueStyle}" Margin="0"/>
                                </Border>
                                
                                <TextBlock Grid.Row="3" Grid.Column="0" Text="Địa chỉ:" Style="{StaticResource InfoLabelStyle}"/>
                                <Border Grid.Row="3" Grid.Column="1" Background="{StaticResource LightGray}" CornerRadius="6" Padding="12,8" Margin="0,0,0,15">
                                    <TextBlock Text="{Binding Address}" Style="{StaticResource InfoValueStyle}" Margin="0"/>
                                </Border>
                                
                                <TextBlock Grid.Row="4" Grid.Column="0" Text="Ngày tham gia:" Style="{StaticResource InfoLabelStyle}"/>
                                <Border Grid.Row="4" Grid.Column="1" Background="{StaticResource LightGray}" CornerRadius="6" Padding="12,8" Margin="0,0,0,15">
                                    <TextBlock Text="{Binding CreatedDate}" Style="{StaticResource InfoValueStyle}" Margin="0"/>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Action Buttons -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="⚙️ Thao tác" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Button Content="🔧 Cài đặt tài khoản" Style="{StaticResource ActionButtonStyle}" Click="AccountSettings_Click"/>
                            <Button Content="📦 Xem đơn hàng" Style="{StaticResource ActionButtonStyle}" Click="ViewOrders_Click"/>
                            <Button Content="🛒 Giỏ hàng" Style="{StaticResource ActionButtonStyle}" Click="ViewCart_Click"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
                
                <!-- Right Content - Recent Orders and Statistics -->
                <StackPanel Grid.Column="1">
                    <!-- Statistics -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📊 Thống kê" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <Border Grid.Column="0" Style="{StaticResource StatCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="📦" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="{Binding TotalOrders}" FontSize="28" FontWeight="Bold" 
                                                   Foreground="{StaticResource PrimaryRed}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Đơn hàng" FontSize="14" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Grid.Column="1" Style="{StaticResource StatCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="💰" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="{Binding TotalSpent}" FontSize="28" FontWeight="Bold" 
                                                   Foreground="{StaticResource PrimaryRed}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Tổng chi tiêu" FontSize="14" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                                
                                <Border Grid.Column="2" Style="{StaticResource StatCardStyle}">
                                    <StackPanel>
                                        <TextBlock Text="👑" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                                        <TextBlock Text="{Binding MembershipLevel}" FontSize="16" FontWeight="Bold" 
                                                   Foreground="{StaticResource PrimaryRed}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="Cấp độ thành viên" FontSize="14" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <!-- Recent Orders -->
                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="📋 Đơn hàng gần đây" Style="{StaticResource SectionTitleStyle}"/>
                            
                            <ItemsControl ItemsSource="{Binding RecentOrders}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource OrderItemStyle}">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding OrderId, StringFormat='Đơn hàng #{0}'}" FontWeight="Bold" FontSize="15"/>
                                                    <TextBlock Text="{Binding OrderDate, StringFormat='📅 Ngày đặt: {0:dd/MM/yyyy HH:mm}'}" FontSize="13" Foreground="{StaticResource DarkGray}" Margin="0,4,0,0"/>
                                                </StackPanel>
                                                
                                                <Border Grid.Column="1" Background="{StaticResource PrimaryRed}" CornerRadius="15" Padding="8,4" Margin="0,0,10,0">
                                                    <TextBlock Text="{Binding Status}" FontSize="12" Foreground="White" FontWeight="Bold"/>
                                                </Border>
                                                
                                                <Button Grid.Column="2" Content="👁️" Background="Transparent" BorderThickness="0" 
                                                        FontSize="16" Padding="8" Cursor="Hand"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            
                            <TextBlock Text="📭 Không có đơn hàng nào" HorizontalAlignment="Center" Foreground="{StaticResource DarkGray}" 
                                       Margin="0,30" x:Name="NoOrdersText" FontSize="16"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
