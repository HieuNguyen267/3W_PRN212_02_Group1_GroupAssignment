<Window x:Class="ShoppingOnline.AdminCategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ShoppingOnline"
        Title="Admin Category Management"
        Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#FFF8F9FA">

    <Window.Resources>
        <!-- Red Theme Colors -->
        <SolidColorBrush x:Key="PrimaryRed" Color="#E31837"/>
        <SolidColorBrush x:Key="DarkRed" Color="#C41230"/>
        <SolidColorBrush x:Key="LightRed" Color="#FFE8EB"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource DarkRed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Quản lý Danh Mục" FontSize="24" FontWeight="Bold" 
                       Foreground="{StaticResource PrimaryRed}"/>
            <TextBlock Text="Danh sách và thông tin danh mục" FontSize="14" Foreground="#FF666666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Main Content -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" 
                BorderBrush="#FFE0E0E0" BorderThickness="1">
            <StackPanel>
                <!-- Search and Buttons -->
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <Grid Grid.Column="0" Margin="0,0,10,0">
                        <TextBox x:Name="CategorySearchBox" Height="35" FontSize="14" 
                                 Padding="10" VerticalContentAlignment="Center"
                                 TextChanged="CategorySearch_TextChanged"/>
                        <TextBlock x:Name="CategorySearchPlaceholder" Text="Tìm kiếm theo tên danh mục..." 
                                   FontSize="14" Foreground="Gray" Margin="15,0,0,0"
                                   VerticalAlignment="Center" HorizontalAlignment="Left"
                                   IsHitTestVisible="False"/>
                    </Grid>

                    <!-- Add Category Button -->
                    <Button Grid.Column="1" Content="Thêm danh mục" Style="{StaticResource ModernButton}" 
                            Height="35" Margin="0,0,10,0" Click="AddCategory_Click"/>

                    <!-- Refresh Button -->
                    <Button Grid.Column="2" Content="Refresh" Style="{StaticResource ModernButton}" 
                            Height="35" Click="RefreshCategories_Click"/>
                </Grid>

                <!-- Categories DataGrid -->
                <DataGrid x:Name="CategoriesDataGrid" 
                        AutoGenerateColumns="False"
                        HeadersVisibility="Column"
                        GridLinesVisibility="Horizontal"
                        RowBackground="Transparent"
                        AlternatingRowBackground="#FFF8F9FA"
                        BorderThickness="1"
                        BorderBrush="#FFE0E0E0"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        CanUserReorderColumns="False"
                        CanUserResizeRows="False"
                        SelectionMode="Single"
                        RowHeight="45">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="100"/>
                        <DataGridTextColumn Header="Tên danh mục" Binding="{Binding CategoryName}" Width="300"/>
                        <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="*"/>
                        <DataGridCheckBoxColumn Header="Kích hoạt" Binding="{Binding IsActive}" Width="100"/>

                        <!-- Actions Column -->
                        <DataGridTemplateColumn Header="Thao tác" Width="200">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="Xem" Style="{StaticResource ModernButton}" 
                                                Height="30" FontSize="11" Margin="2" Width="50"
                                                Click="ViewCategory_Click" Tag="{Binding CategoryId}"/>
                                        <Button Content="Sửa" Style="{StaticResource ModernButton}" 
                                                Height="30" FontSize="11" Margin="2" Width="50"
                                                Background="#FFFF9800"
                                                Click="EditCategory_Click" Tag="{Binding CategoryId}"/>
                                        <Button Content="Xóa" Style="{StaticResource ModernButton}" 
                                                Height="30" FontSize="11" Margin="2" Width="50"
                                                Background="#FFF44336"
                                                Click="DeleteCategory_Click" Tag="{Binding CategoryId}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock x:Name="CategoryCountText" Text="Tổng: 0 danh mục" FontSize="14" 
                       VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Quay lại Dashboard" Style="{StaticResource ModernButton}" 
                    Background="#FF28A745" Margin="0,0,10,0" Click="BackToDashboard_Click"/>
            <Button Content="Đóng" Style="{StaticResource ModernButton}" 
                    Background="#FF6C757D" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
