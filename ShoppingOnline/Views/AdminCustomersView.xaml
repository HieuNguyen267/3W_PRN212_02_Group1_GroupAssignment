<UserControl x:Class="ShoppingOnline.Views.AdminCustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search and Filter Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <TextBox x:Name="CustomerSearchBox" Height="35" FontSize="14" 
                         Padding="10" VerticalContentAlignment="Center"
                         TextChanged="CustomerSearch_TextChanged"/>
                <TextBlock x:Name="CustomerSearchPlaceholder" Text="Tim kiem theo ten khach hang, email, so dien thoai..." 
                           FontSize="14" Foreground="Gray" Margin="15,0,0,0"
                           VerticalAlignment="Center" HorizontalAlignment="Left"
                           IsHitTestVisible="False"/>
            </Grid>

            <!-- Status Filter -->
            <ComboBox x:Name="StatusFilterComboBox" Grid.Column="1" Width="150" Height="35" 
                      FontSize="14" Margin="0,0,10,0"
                      SelectionChanged="StatusFilter_Changed">
                <ComboBoxItem Content="Tat ca trang thai" IsSelected="True"/>
                <ComboBoxItem Content="Hoat dong"/>
                <ComboBoxItem Content="Khoa"/>
            </ComboBox>

            <!-- Refresh Button -->
            <Button Grid.Column="2" Content="Refresh" Height="35" 
                    Click="RefreshCustomers_Click" Style="{DynamicResource ModernButton}"/>
        </Grid>

        <!-- Customers DataGrid -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" 
                BorderBrush="#FFE0E0E0" BorderThickness="1">
            <DataGrid x:Name="CustomersDataGrid" 
                    AutoGenerateColumns="False"
                    HeadersVisibility="Column"
                    GridLinesVisibility="Horizontal"
                    RowBackground="Transparent"
                    AlternatingRowBackground="#FFF8F9FA"
                    BorderThickness="1"
                    BorderBrush="#FFE0E0E0"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="False"
                    CanUserResizeRows="False"
                    SelectionMode="Single"
                    RowHeight="50">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding CustomerId}" Width="80"/>
                    <DataGridTextColumn Header="Ten khach hang" Binding="{Binding FullName}" Width="200"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Account.Email}" Width="200"/>
                    <DataGridTextColumn Header="So dien thoai" Binding="{Binding Phone}" Width="150"/>
                    <DataGridTextColumn Header="Dia chi" Binding="{Binding Address}" Width="250"/>
                    <DataGridTextColumn Header="Ngay tao" Binding="{Binding CreatedDate, StringFormat=dd/MM/yyyy}" Width="120"/>
                    <DataGridTextColumn Header="Trang thai" Binding="{Binding Account.IsActive}" Width="100"/>
                    
                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Thao tac" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Xem" Height="30" FontSize="11" Margin="2" Width="50"
                                            Click="ViewCustomer_Click" Tag="{Binding CustomerId}" 
                                            Style="{DynamicResource ModernButton}"/>
                                    <Button Content="Sua" Height="30" FontSize="11" Margin="2" Width="50"
                                            Background="#FFFF9800" Click="EditCustomer_Click" Tag="{Binding CustomerId}"
                                            Style="{DynamicResource ModernButton}"/>
                                    <Button Content="Xoa" Height="30" FontSize="11" Margin="2" Width="50"
                                            Background="#FFF44336" Click="DeleteCustomer_Click" Tag="{Binding CustomerId}"
                                            Style="{DynamicResource ModernButton}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock x:Name="CustomerCountText" Text="Tong: 0 khach hang" FontSize="14" 
                       VerticalAlignment="Center" Margin="0,0,20,0"/>
        </StackPanel>
    </Grid>
</UserControl>