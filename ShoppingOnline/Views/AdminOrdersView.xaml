<UserControl x:Class="ShoppingOnline.Views.AdminOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search and Filter Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <TextBox x:Name="OrderSearchBox" Height="35" FontSize="14" 
                         Padding="10" VerticalContentAlignment="Center"
                         TextChanged="OrderSearch_TextChanged"/>
                <TextBlock x:Name="SearchPlaceholder" Text="Tim kiem theo ID don hang, ten khach hang..." 
                           FontSize="14" Foreground="Gray" Margin="15,0,0,0"
                           VerticalAlignment="Center" HorizontalAlignment="Left"
                           IsHitTestVisible="False"/>
            </Grid>

            <!-- Status Filter -->
            <ComboBox x:Name="StatusFilterComboBox" Grid.Column="1" Width="150" Height="35" 
                      FontSize="14" Margin="0,0,10,0"
                      SelectionChanged="StatusFilter_Changed">
                <ComboBoxItem Content="Tat ca trang thai" IsSelected="True"/>
                <ComboBoxItem Content="Pending"/>
                <ComboBoxItem Content="Confirmed"/>
                <ComboBoxItem Content="Shipping"/>
                <ComboBoxItem Content="Delivered"/>
                <ComboBoxItem Content="Cancelled"/>
            </ComboBox>

            <!-- Date Filter -->
            <ComboBox x:Name="DateFilterComboBox" Grid.Column="2" Width="120" Height="35" 
                      FontSize="14" Margin="0,0,10,0"
                      SelectionChanged="DateFilter_Changed">
                <ComboBoxItem Content="Tat ca" IsSelected="True"/>
                <ComboBoxItem Content="Hom nay"/>
                <ComboBoxItem Content="7 ngay qua"/>
                <ComboBoxItem Content="30 ngay qua"/>
            </ComboBox>

            <!-- Refresh Button -->
            <Button Grid.Column="3" Content="Refresh" Height="35" 
                    Click="RefreshOrders_Click" Style="{DynamicResource ModernButton}"/>
        </Grid>

        <!-- Orders DataGrid -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" 
                BorderBrush="#FFE0E0E0" BorderThickness="1">
            <DataGrid x:Name="OrdersDataGrid" 
                    AutoGenerateColumns="False"
                    HeadersVisibility="Column"
                    GridLinesVisibility="Horizontal"
                    RowBackground="Transparent"
                    AlternatingRowBackground="#FFF8F9FA"
                    BorderThickness="1"
                    BorderBrush="#FFE0E0E0"
                    CanUserAddRows="False"
                    CanUserDeleteRows="False"
                    CanUserReorderColumns="False"
                    CanUserResizeRows="False"
                    SelectionMode="Single"
                    RowHeight="50">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding OrderId}" Width="80"/>
                    <DataGridTextColumn Header="Khach hang" Binding="{Binding Customer.FullName}" Width="200"/>
                    <DataGridTextColumn Header="Ngay dat" Binding="{Binding OrderDate, StringFormat=dd/MM/yyyy HH:mm}" Width="150"/>
                    <DataGridTextColumn Header="Tong tien" Binding="{Binding TotalAmount, StringFormat={}{0:N0}d}" Width="120"/>
                    <DataGridTextColumn Header="Trang thai" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Dia chi" Binding="{Binding ShippingAddress}" Width="250"/>
                    
                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Thao tac" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="Xem" Height="30" FontSize="11" Margin="2" Width="50"
                                            Click="ViewOrderDetail_Click" Tag="{Binding OrderId}" 
                                            Style="{DynamicResource ModernButton}"/>
                                    <Button Content="Xac nhan" Height="30" FontSize="11" Margin="2" Width="60"
                                            Background="#FF4CAF50" Click="ConfirmOrder_Click" Tag="{Binding OrderId}"
                                            Style="{DynamicResource ModernButton}"/>
                                    <Button Content="Huy" Height="30" FontSize="11" Margin="2" Width="50"
                                            Background="#FFF44336" Click="CancelOrder_Click" Tag="{Binding OrderId}"
                                            Style="{DynamicResource ModernButton}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>