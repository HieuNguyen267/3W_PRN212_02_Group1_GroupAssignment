<UserControl x:Class="ShoppingOnline.Views.AdminCarriersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:ShoppingOnline.Views">
    
    <UserControl.Resources>
        <!-- Converters for status display -->
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        
        <!-- Custom converters for better display -->
        <local:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
        <local:BoolToToggleButtonConverter x:Key="BoolToToggleButtonConverter"/>
        <local:BoolToToggleButtonBackgroundConverter x:Key="BoolToToggleButtonBackgroundConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search and Filter Bar -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <Grid Grid.Column="0" Margin="0,0,10,0">
                <TextBox x:Name="CarrierSearchBox" Style="{StaticResource AdminTextBox}"
                         TextChanged="CarrierSearch_TextChanged"/>
                <TextBlock x:Name="CarrierSearchPlaceholder" Text="Search by carrier name, email, phone..." 
                           Style="{StaticResource AdminMutedText}" Margin="15,0,0,0"
                           VerticalAlignment="Center" HorizontalAlignment="Left"
                           IsHitTestVisible="False"/>
            </Grid>

            <!-- Status Filter -->
            <ComboBox x:Name="StatusFilterComboBox" Grid.Column="1" Width="150" 
                      Style="{StaticResource AdminComboBox}" Margin="0,0,10,0"
                      SelectionChanged="StatusFilter_Changed">
                <!-- Use Tag as the stable value to avoid localization issues -->
                <ComboBoxItem Content="All Status" Tag="All" IsSelected="True"/>
                <ComboBoxItem Content="Active" Tag="Active"/>
                <ComboBoxItem Content="Locked" Tag="Locked"/>
            </ComboBox>

            <!-- Add Carrier Button -->
            <Button Grid.Column="2" Content="Add Carrier" Width="100" Margin="0,0,10,0"
                    Click="AddCarrier_Click" Style="{StaticResource AdminSuccessButton}"/>

            <!-- Refresh Button -->
            <Button Grid.Column="3" Content="Refresh" Width="80"
                    Click="RefreshCarriers_Click" Style="{StaticResource AdminModernButton}"/>
        </Grid>

        <!-- Carriers DataGrid -->
        <Border Grid.Row="1" Style="{StaticResource AdminCard}">
            <DataGrid x:Name="CarriersDataGrid" Style="{StaticResource AdminDataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding CarrierId}" Width="80"/>
                    <DataGridTextColumn Header="Carrier Name" Binding="{Binding FullName}" Width="180"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Account.Email}" Width="200"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                    <DataGridTextColumn Header="Vehicle Number" Binding="{Binding VehicleNumber}" Width="100"/>
                    <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedDate, StringFormat=dd/MM/yyyy}" Width="100"/>
                    
                    <!-- Status Column -->
                    <DataGridTemplateColumn Header="Status" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Text>
                                        <Binding Path="Account.IsActive" Converter="{StaticResource BoolToStatusConverter}"/>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- Available Status Column -->
                    <DataGridTemplateColumn Header="Available" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock.Text>
                                        <Binding Path="IsAvailable" Converter="{StaticResource BoolToStatusConverter}"/>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Actions" Width="320">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="View" Width="50" Margin="2"
                                            Click="ViewCarrier_Click" Tag="{Binding CarrierId}" 
                                            Style="{StaticResource AdminSmallButton}"
                                            Background="{StaticResource AdminInfoBlue}"/>
                                    <Button Content="Edit" Width="50" Margin="2"
                                            Click="EditCarrier_Click" Tag="{Binding CarrierId}"
                                            Style="{StaticResource AdminSmallButton}"
                                            Background="{StaticResource AdminWarningOrange}"/>
                                    <Button Content="Delete" Width="50" Margin="2"
                                            Click="DeleteCarrier_Click" Tag="{Binding CarrierId}"
                                            Style="{StaticResource AdminSmallButton}"
                                            Background="{StaticResource AdminDangerRed}"/>
                                    <Button Width="60" Margin="2"
                                            Click="ToggleCarrierAvailable_Click" Tag="{Binding CarrierId}"
                                            Content="{Binding IsAvailable, Converter={StaticResource BoolToToggleButtonConverter}}"
                                            Background="{Binding IsAvailable, Converter={StaticResource BoolToToggleButtonBackgroundConverter}}"
                                            Style="{StaticResource AdminSmallButton}"/>
                                    <Button Width="60" Margin="2"
                                            Click="ToggleCarrierStatus_Click" Tag="{Binding CarrierId}"
                                            Style="{StaticResource AdminSmallButton}">
                                        <Button.Content>
                                            <Binding Path="Account.IsActive" Converter="{StaticResource BoolToToggleButtonConverter}"/>
                                        </Button.Content>
                                        <Button.Background>
                                            <Binding Path="Account.IsActive" Converter="{StaticResource BoolToToggleButtonBackgroundConverter}"/>
                                        </Button.Background>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock x:Name="CarrierCountText" Text="Total: 0 carriers" Style="{StaticResource AdminBodyText}" 
                       VerticalAlignment="Center" Margin="0,0,20,0"/>
        </StackPanel>
    </Grid>
</UserControl>