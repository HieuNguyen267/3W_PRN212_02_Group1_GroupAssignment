<Window x:Class="ShoppingOnline.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShoppingOnline"
        mc:Ignorable="d"
        Title="Đăng nhập" Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryRed" Color="#E31837"/>
        <SolidColorBrush x:Key="LightGray" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="DarkGray" Color="#666666"/>
        <SolidColorBrush x:Key="MediumGray" Color="#E0E0E0"/>

        <!-- Styles -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,30"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="{StaticResource MediumGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="{StaticResource MediumGray}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryRed}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LightGray}">
        <Border Background="White" Width="320" Height="380" CornerRadius="10" 
                BorderBrush="{StaticResource MediumGray}" BorderThickness="1">
            <StackPanel Margin="30" VerticalAlignment="Center">
                <!-- Logo/Brand -->
                <TextBlock Text="cellphoneS" Style="{StaticResource HeaderStyle}"/>
                
                <!-- Login Form -->
                <StackPanel>
                    <TextBlock Text="Email:" Style="{StaticResource LabelStyle}"/>
                                         <TextBox x:Name="UsernameTextBox" Style="{StaticResource TextBoxStyle}" 
                              Text="nguyenduyhieu@gmail.com"/>
                    
                    <TextBlock Text="Mật khẩu:" Style="{StaticResource LabelStyle}"/>
                    <PasswordBox x:Name="PasswordBox" Password="123456" Style="{StaticResource PasswordBoxStyle}"/>
                    
                    <!-- Remember Me -->
                    <CheckBox Content="Ghi nhớ đăng nhập" Margin="0,0,0,20" FontSize="12"/>
                    
                    <!-- Login Button -->
                    <Button Content="Đăng nhập" Style="{StaticResource LoginButtonStyle}" 
                            Click="Login_Click" Margin="0,0,0,15"/>
                    
                    <!-- Links -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="Quên mật khẩu?" Style="{StaticResource LinkButtonStyle}" 
                                Click="ForgotPassword_Click" Margin="0,0,10,0"/>
                        <TextBlock Text="|" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Button Content="Đăng ký" Style="{StaticResource LinkButtonStyle}" 
                                Click="Register_Click"/>
                    </StackPanel>
                </StackPanel>
                
                <!-- Close Button -->
                <Button Content="Đóng" Background="{StaticResource MediumGray}" 
                        Foreground="Black" Height="35" Margin="0,20,0,0" 
                        Click="Close_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
